import 'package:flutter/material.dart';

// Ce fichier contient toutes les couleurs
// de notre application
// C'est comme les variables CSS !

class AppColors {

  // 🔵 Couleur principale (le bleu de ton design Figma)
  static const Color primary = Color(0xFF1E3A8A);

  // ⚫ Couleur du texte principal (presque noir)
  static const Color textDark = Color(0xFF1F2937);

  // 🩶 Couleur du texte secondaire (gris)
  static const Color textMedium = Color(0xFF6B7280);

  // ⬜ Couleur de fond (blanc)
  static const Color background = Color(0xFFFFFFFF);

  // ⬜ Couleur de fond clair (gris très clair)
  static const Color backgroundLight = Color(0xFFF9FAFB);

  // 🔴 Priorité Élevée = Rouge
  static const Color priorityHigh = Color(0xFFEF4444);

  // 🟠 Priorité Moyenne = Orange
  static const Color priorityMedium = Color(0xFFF59E0B);

  // 🟢 Priorité Basse = Vert
  static const Color priorityLow = Color(0xFF10B981);

  // ⬜ Couleur des bordures
  static const Color border = Color(0xFFE5E7EB);

}